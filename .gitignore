*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
.env

# Signing material. The debug keystore is regenerated locally per README.
debug.keystore
*.jks

# User financial data. An encrypted backup or a pulled database must never be
# committed — the passphrase protects the contents, but the file is still the
# user's complete ledger.
*.avault
*.db
*.db-journal
*.db-wal

# Scratch and generated debris
/arth-vault.zip
/sqlc/
/assets/.aistudio/
